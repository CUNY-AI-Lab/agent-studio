# The active Agent Studio app does not use a repo-root `.env` file.
#
# Use these instead:
# - `cloudflare/.dev.vars.example` for the Worker runtime
# - `frontend/.env.example` for split frontend development
#
# This file does not describe any deployed environment and must not be used to
# infer production configuration.
